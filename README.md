# VersionInfoApp

## 概要
- .NET 8 の新機能を使用して Git のコミットハッシュを含めたバージョン情報を表示
- xUnit を使用したテストを実装

## 設定・便利機能
- **`Directory.Build.props`** で `GitCommitIdShort` をバージョン情報に含める

## 使い方
1. `git clone https://github.com/hajimenishida/VersionInfoApp.git`
2. `dotnet run`
